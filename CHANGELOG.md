# CHANGELOG

Tampermonkey 스피또 사이니지 프로젝트의 기능 추가, 수정 및 운영 변경이력을 기록합니다.

---

## 2026-08-15

### 프로젝트 독립 관리 시작

- 프로젝트명 `tampermonkey-spitto-signage` 확정
- 기존 Tampermonkey 단독 운영 코드를 독립 프로젝트로 분리
- GitHub 저장 및 변경이력 관리 준비
- 프로젝트 기본 구조 생성
  - `spitto-signage.user.js`
  - `README.md`
  - `CHANGELOG.md`
  - `.gitignore`
  - `docs/screenshots/`

### 판매중 스피또 수집

- 동행복권 스피또 발행내역 페이지에서 판매중 상품 자동 탐색
- 판매종료 상품 제외
- 현재 페이지에서 판매중 카드가 부족할 경우 다음 페이지 탐색
- 최대 5개 카드 수집 및 표시
- 중복 상품 수집 방지

### 사이니지 오버레이 방식

- 동행복권 원본 페이지를 직접 삭제하거나 크게 변경하는 방식 폐기
- 별도 전체화면 오버레이 생성 방식으로 변경
- 원본 페이지 로딩 구조와 사이니지 화면을 분리
- PC → HDMI 출력 환경을 기준으로 화면 구성

### 카드 디자인

- 판매중 상품별 독립 카드 생성
- 회색 라운드 테두리 적용
- 상품명 표시
- 판매중 상태 배지 표시
- 회차번호 표시
- 상품 이미지 표시

### 상세정보 표시

다음 항목 표시:

- 1등 당첨금
- 판매가격
- 판매점 입고율
- 1등 잔여수량
- 2등 잔여수량
- 각 항목 기준일

### 데이터 표시 보정

- 회차번호가 `661`, `651`, `000` 등으로 잘못 추출되던 문제 수정
- `제66회` 형태 우선 추출 방식 적용
- `10억원`, `5억원` 등 1등 당첨금 파싱 지원
- 기준일이 여러 번 중복 표시되던 문제 수정
- 각 항목에서 가장 가까운 기준일 1개만 표시

### 색상 표시

- 판매점 입고율 청록색 강조
- 잔여수량 분자 청록색 표시
- 잔여수량 `/` 및 분모 검정색 표시
- 일반 금액 및 수량 값 검정색 표시
- 기준일 회색 표시

### 가독성 개선

매장 HDMI 화면을 멀리서 확인할 수 있도록 글자 가독성 강화.

- Noto Sans KR 사용
- 제목/정보 값 굵기 증가
- 글자 간격 조정
- text-shadow 기반 글자 두께 보정
- 카드 크기 및 카드 간격 현장 조정 가능하도록 상수화

### 카드 표시 개수 변경

초기 4개 표시에서 현재 5개 표시로 변경.

```javascript
const MAX = 5;
```

5개 카드 배치에 맞춰 카드 폭 조정.

```javascript
const CARD_WD = 650;
```

### 현재시각 표시

사이니지 상단에 실시간 날짜/시간 표시 추가.

표시 형식:

```text
오후 6:28:37 / 2026년 4월 18일(토)
```

- 오전/오후 12시간제
- 시/분/초 표시
- 연/월/일 표시
- 요일 표시
- 1초마다 자동 갱신

### 카드 위치 조정

상단 시계 공간 확보를 위해 카드 영역을 화면 아래쪽으로 이동할 수 있도록 설정 추가.

```javascript
const CLOCK_BOTTOM_GAP = 70;
const CARDS_PUSH_DOWN = 40;
```

### 자동 새로고침

동행복권 최신 정보 반영을 위해 240분마다 페이지 자동 새로고침.

```javascript
const REFRESH_MINUTES = 240;
```

---

## 개발 초기 이력

독립 GitHub 프로젝트 생성 이전에는 Tampermonkey 편집기에서 직접 개발·수정했습니다.

주요 시행착오:

- 원본 페이지 요소를 직접 숨기는 방식에서 화면 전체가 사라지는 문제 발생
- 원본 `#galleryDiv`를 직접 변경하는 방식에서 페이지 로딩 및 배치 문제 발생
- 원본 ID를 복제하여 사용하는 방식에서 CSS/DOM 충돌 발생
- 최종적으로 원본 페이지는 그대로 유지하고 별도 오버레이를 생성하는 방식으로 구조 확정

이전 개발 이력 중 GitHub 프로젝트 생성 전에 이루어진 세부 실험 코드는 별도로 보존하지 않습니다.